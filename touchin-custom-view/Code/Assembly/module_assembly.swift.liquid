enum {{ module_info.name }}ModuleAssembly {

    static func {{ module_info.name | prepend:'create' }}Module() -> {{ module_info.name }}Module {
        let viewModel = {{ module_info.name }}ViewModel()
        return {{ module_info.name }}ViewController(viewModel: viewModel)
    }

}
